{
  "name": "lattice",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cli": "bun run src/cli.ts",
    "capture": "bun run src/cli.ts capture",
    "synthesize": "bun run src/cli.ts synthesize",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "verify": "bun test && bun run src/cli.ts verify"
  },
  "dependencies": {
    "@github/copilot-sdk": "^1.0.0",
    "commander": "^15.0.0",
    "openai": "^6.42.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.3"
  }
}
